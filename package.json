{
  "name": "nodejs-sonar-app",
  "version": "1.0.0",
  "description": "Node.js app with GitHub Actions, SonarQube, and deployment",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "build": "echo 'Building project...' && exit 0",
    "sonar": "sonar-scanner"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "jest-environment-jsdom": "^29.6.1",
    "sonarqube-scanner": "^2.8.0"
  }
}
